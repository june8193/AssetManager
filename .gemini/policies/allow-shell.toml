[[rule]]
# shell 실행 도구를 대상으로 지정
toolName = "run_shell_command"

# 별도 확인 없이 즉시 실행 허용
decision = "allow"

# 높은 우선순위를 설정하여 기본 정책을 덮어씀
priority = 999

# 파이프나 리다이렉션(>, >>)이 포함된 커맨드도 확인 없이 허용
allowRedirection = true
