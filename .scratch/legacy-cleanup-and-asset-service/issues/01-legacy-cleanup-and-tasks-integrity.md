# 01 — 레거시 진입점 정리 및 백그라운드 태스크 무결성 확보

**What to build:** 미사용 과거 서버 진입점을 완전히 제거하여 백엔드 실행 경로를 단일화하고, 백그라운드 태스크 관리자의 예외 처리 오타 버그를 수정하여 종목 동기화 실패 시 발생하는 2차 런타임 크래시를 방지합니다. 또한 FastAPI 서버 수명주기와 시스템 상태 API 간 태스크 관리자 싱글톤 인스턴스를 일원화합니다.

**Blocked by:** None — can start immediately

**Status:** resolved

- [x] 미사용 과거 엔트리포인트 파일(`src/server/main.py`)이 완전히 삭제되고 메인 백엔드 진입점으로 일원화된다.
- [x] 백그라운드 태스크 관리자 유지보수 루프 내 에러 업데이트 호출 오타(`self._update_task_error` -> `self.update_task_error`)가 수정된다.
- [x] 서버 수명주기(FastAPI lifespan)와 시스템 상태 조회 라우터 간 동일한 전역 싱글톤 태스크 관리자 인스턴스가 공유된다.
- [x] 백그라운드 태스크 관리자의 성공/에러 상태 기록 및 싱글톤 동작을 검증하는 단위 테스트(`tests/test_tasks.py`)가 통과한다.
