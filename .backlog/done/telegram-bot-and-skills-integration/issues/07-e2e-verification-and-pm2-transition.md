# 07 — 통합 E2E 검증, 기존 프로세스 정리 및 단일 서버 운영 전환 슬라이스

**What to build:**
통합 완료된 전체 백엔드 시스템에 대한 단위/회귀 테스트 통과를 확인하고, 실제 프로덕션 서버 환경에서 텔레그램 메시지 송수신 및 CLI 커맨드를 실측 검증한 뒤, 기존 PM2 데몬 프로세스를 안전하게 종료하고 `AssetManager` 단일 서버 운영 체제로 성공적으로 전환합니다.

**Blocked by:** 05 — FastAPI Lifespan 단일 서버 통합, 장애 알림 및 환경 격리 슬라이스, 06 — 리포트 5종 에이전트 스킬 및 보조 CLI 스크립트 이관 슬라이스

**Status:** resolved

- [x] 전체 백엔드 단위 테스트 세트(`uv run pytest`)가 100% 통과하여 기존 기능에 대한 회귀 버그가 없음을 검증한다.
- [x] 개발 서버(`uv run scripts/dev.py`) 구동 시 텔레그램 봇 충돌 없이 웹 UI 및 백엔드 API가 정상 동작함을 검증한다.
- [x] 프로덕션 모드 환경에서 텔레그램 명령어가 실시간으로 정상 응답함을 E2E로 검증한다.
- [x] PM2 데몬 프로세스에서 기존 `asset-jun-bot`을 안전하게 중지(`pm2 stop asset-jun-bot`)하고, `AssetManager` 프로세스만 상시 가동되도록 설정한다.
- [x] `Asset-jun-bot` 레포지토리는 로컬 자산 상담/감사용으로 안전하게 보존하고 문서 및 백로그 상태를 완료 처리한다.
