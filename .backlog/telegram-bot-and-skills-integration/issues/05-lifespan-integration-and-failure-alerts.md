# 05 — FastAPI Lifespan 단일 서버 통합, 장애 알림 및 환경 격리 슬라이스

**What to build:**
`AssetManager` 백엔드 서버가 구동될 때 프로덕션 환경(`APP_ENV=production`)에서 텔레그램 봇 롱폴링과 스케줄러가 백그라운드 태스크로 자동 실행되고, 서버 백그라운드 태스크(시세 업데이트, DB 백업, 종목 동기화)에 장애가 발생하면 텔레그램으로 긴급 알림을 즉각 발송하며, 개발(`dev.py`) 및 테스트(`pytest`) 환경에서는 봇을 격리하여 409 Conflict 오류를 방지하는 E2E 슬라이스를 구축합니다.

**Blocked by:** 04 — 키움 거래내역 수동/자동 동기화 (/sync, 18:10/07:10 스케줄러) 슬라이스

**Status:** ready-for-agent

- [ ] `src/backend/main.py`의 `lifespan` 시작 시 프로덕션 환경 또는 활성화 플래그에 따라 텔레그램 봇을 백그라운드 태스크로 기동한다.
- [ ] 서버 종료 시 롱폴링 및 스케줄러 태스크가 안전하게 취소(`cancel()`)되고 리소스가 누수 없이 정리된다.
- [ ] `pytest` 테스트 및 `scripts/dev.py` 구동 환경에서는 텔레그램 봇이 활성화되지 않도록 자동 격리 가드가 적용된다.
- [ ] 5분 주기로 백엔드 태스크 상태를 조회하여 `failed` 상태 및 에러 메시지가 발견되면 등록된 모든 사용자에게 긴급 장애 알림을 발송한다.
- [ ] `lifespan` 구동/종료 수명주기 및 백엔드 태스크 장애 알림 감시 로직을 검증하는 단위 테스트가 통과한다.
